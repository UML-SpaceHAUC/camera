#!/usr/bin/expect -f

set NAME [lindex $argv 0];

if { $NAME == ""} {
set FILENAME "test";
} else {
set FILENAME "$NAME";
}

spawn scp -P8823 pi@spacehauc.ddns.net:~/cameraDemp/$FILENAME.jpg /home/osboxes/cam/camera

expect "assword:"
send "raspberry\r"
interact
